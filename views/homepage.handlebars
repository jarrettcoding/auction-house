<ol class="post-list">
  <li>
    <article class="post">
      <div class="title">
        <p>{{id}}</p>
        <p>{{product_name}}</p>
        <p>{{price}}</p>
        <p>{{stock}}</p>
        <p>{{description}}</p>
        <p>{{image}}</p>
        <p>{{category_id}}</p>
      </div>
      {{!-- <div class="meta">
        {{vote_count}} point(s) by {{user.username}} on
        {{created_at}}
        |
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
      </div> --}}
    </article>
  </li>
</ol>