

 <div class="slideshow-container">
   <div class="slides">
   <div class="mySlides fade" id="preview">
 
 
    </div>
   </div>
 </div>

 <!-- The dots/circles -->
<div style="text-align:center">
  <span class="dot" onclick="currentSlide(1)"></span>
  <span class="dot" onclick="currentSlide(2)"></span>
  <span class="dot" onclick="currentSlide(3)"></span>
</div>
  
<ol class="post-list">
  {{#each products}}
  <oi>
    <article class="post">
      <div class="title">
        <h1>{{product_name}}</h1>
        <p>Product Id: {{id}}</p>
        <p>Product Name: {{product_name}}</p>
        <p>Product Price: {{price}}</p>
        <p>Product Stock: {{stock}}</p>
        <p>Product Description: {{description}}</p>
        {{!-- Neeeds anchor tag along with the photos with the path --}}
        <p>Product Image: {{image}}</p>
        <p>Product Category: {{category_id}}</p>
      </div>
      {{!-- <div class="meta">
        {{vote_count}} point(s) by {{user.username}} on
        {{created_at}}
        |
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
      </div> --}}
    </article>
     {{/each}}
  </ol>
</ol>

<script src="/javascript/product-info.js"></script>
