 <div id="hero">
   <div class="navbar">
     <div id ='image'>
       {{!-- images feed from the slider.js --}}
       </div>
   </div>
 </div>

 <div class="slider">
   <img src="res/left-arrow.png" id="prev">
   <div class="preview" id="preview">
 
 
    </div>
    <button id="next">clickme</button>
   <img src="res/right-arrow.png" id="">

 </div>
  
<ol class="post-list">
  {{#each products}}
  <oi>
    <article class="post">
      <div class="title">
        <h1>{{product_name}}</h1>
        <p>Product Id: {{id}}</p>
        <p>Product Name: {{product_name}}</p>
        <p>Product Price: {{price}}</p>
        <p>Product Stock: {{stock}}</p>
        <p>Product Description: {{description}}</p>
        {{!-- Neeeds anchor tag along with the photos with the path --}}
        <p>Product Image: {{image}}</p>
        <p>Product Category: {{category_id}}</p>
      </div>
      {{!-- <div class="meta">
        {{vote_count}} point(s) by {{user.username}} on
        {{created_at}}
        |
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
      </div> --}}
    </article>
     {{/each}}
  </ol>
</ol>
<script src="/javascript/slider.js"></script>